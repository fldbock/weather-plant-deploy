apiVersion: v1
kind: Service
metadata:
  name: {{  .Values.api.name  }}
  labels:
    name: {{  .Values.api.name  }}
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    name: {{  .Values.api.name  }}
